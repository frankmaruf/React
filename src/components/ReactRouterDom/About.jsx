import React from 'react'

const About = (props) => {
    return (
        <React.Fragment>
            <h1>Hello, This is about {props.name}</h1>
        </React.Fragment>
    )
}

export default About
